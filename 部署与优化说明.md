# 部署与优化说明（Cloudflare Pages + Worker 优选）

> 部署日期：2026-09-19 ｜ 正式地址：<https://tcm.xihale.top> ｜ 备用：`https://tcm-test-git.pages.dev`、`https://tcm-test-proxy.xihale.workers.dev`
> 方案参考：[Cloudflare Worker 国内 IP 优选加速实践](https://monkeywie.cn/posts/cf-worker-fast-ip)（monkeywie/Levi）

## 一、架构

```
访客(广东移动等)
   │ DNS: tcm.xihale.top ──灰云CNAME──► youxuanz.cf.090227.xyz（社区优选域名，持续更新国内最快的CF IP）
   │ 实测解析落地：香港 HKG（TCP握手 ~13-20ms）
   ▼
Cloudflare 边缘（香港）── Worker Route: tcm.xihale.top/* ──► tcm-test-proxy Worker
   │                                                          │ Cache API：/assets/* 永久缓存
   │                                                          │            HTML 缓存 300 秒
   └────────────────────── 回源 ──────────────────────────────┘
                                                            ▼
                                              Cloudflare Pages (tcm-test-git)
```

要点：
- **Pages 自定义域不可用于优选**：Pages 强制要求自定义域的 DNS 记录走橙云代理，切灰云会被置为 `deactivated`（边缘报 "DNS points to prohibited IP"）。因此入口改为 **Worker Route**（按 Host 匹配，不依赖橙云状态）+ 反代到 Pages。
- **DNS 完全由优选域名控制**：`tcm` 记录是灰云 CNAME → `youxuanz.cf.090227.xyz`，IP 漂移由社区域名自动跟进，无需手动换 IP。
- DNS 托管在 Cloudflare（xihale.top 的 zone 在 CF 账号下），灰云 CNAME 指向外部优选域名是允许的。

## 二、日常操作

仓库托管在 GitHub（`xihale/tcm-test`，MIT 开源）。2026-09-19 起前端从 `wrangler pages deploy` 直传切换为 **Pages Git 集成：push 到 main 后 Pages 自动拉取、构建并发布**，本地不再需要手动部署前端。

```bash
# 前端发版：改完代码直接 push（Pages 自动构建 npm run build → 发布 dist）
git push

# Worker 不走 Git 集成，仍是本地手动发（改 worker/index.js 后）：
npx wrangler deploy
```

注意：
- Worker 对 HTML 有 **300 秒边缘缓存**：部署新版本后，各节点最长 5 分钟才看到新页面。急发可在 Dashboard → Workers → tcm-test-proxy 清缓存，或等 5 分钟。
- Worker 免费额度 10 万请求/天，静态站绰绰有余。
- Pages 构建环境 Node 版本跟随 CF 默认（22.x），vite 6 无需特殊配置；要固定可在项目 Settings → 环境变量加 `NODE_VERSION`。

## 三、文件清单

| 文件 | 作用 |
|------|------|
| `vite.config.ts` | manualChunks 分包：echarts（仅结果页异步加载）、naive-ui、vue+pinia 独立分包，首屏并行下载 |
| `public/_headers` | `/assets/*` 一年 `immutable`；全站 nosniff / Referrer-Policy |
| `worker/index.js` | 反代 Pages + 边缘缓存（HTML 300s / immutable 资源永久）+ 重定向域名改写 |
| `wrangler.jsonc` | Worker 配置（tcm-test-proxy） |
| `scripts/find-colo.sh` | CF IP 段落地探测脚本（优选失效时重扫用） |

## 四、实测数据（2026-09-19，广东移动出口）

| 项目 | 数据 |
|------|------|
| 优选域名解析 | `youxuanz.cf.090227.xyz` → 104.18.35.15 / 172.64.152.241 → **落地 HKG** |
| TCP 握手（到 HKG） | 13–20ms（对照：默认路由到 LAX ~167ms） |
| 下午（非峰值）整链路首字节 | 65–91ms |
| 晚高峰（19:40 前后）首字节 | 350–470ms（同 IP 直连 pages.dev 也为 370ms+，属国际链路高峰拥堵，非架构开销） |
| Worker 层额外开销 | ~30ms（缓存命中路径 vs 纯边缘路径对比） |
| 默认路由（未优化）同期 | 550–1050ms |
| 静态资源 | 经 Worker 边缘缓存，`immutable` 一年 |

结论：晚高峰整条国际链路（包括 Cloudflare 自家域名）都会劣化 200–400ms，优选方案保住了"最近入口 + 边缘缓存"的底盘；非峰值时段接近直连香港的极限速度。

## 五、维护

- **感觉变慢时**：先跑 `./scripts/find-colo.sh`（或直接 `dig youxuanz.cf.090227.xyz` 后逐 IP 探测 colo）确认优选域名当前解析是否还落地香港/低延迟节点；社区域名失效的话，换 CNAME 目标（如 `cf.090227.xyz` 其他子域）或临时灰云 A 记录指向自扫的最优 IP（如 `104.19.32.0/23` 段）。
- **pages.dev 域名**：部分地区被 DNS 污染，勿作为正式入口；Worker 的 workers.dev 域名同理，仅作调试备用。
- 本机解析不通时先用 `dig @223.5.5.5` 区分是站点问题还是运营商 DNS 负缓存（新记录常见，TTL 过期自愈）。

## 六、踩坑记录

1. **Pages 自定义域 + 灰云记录 → 域名被自动 deactivated**（403 "DNS points to prohibited IP"）：Pages 检测到自定义域的 DNS 记录脱离橙云代理即停用。这就是为什么入口必须走 Worker Route 而不是 Pages 自定义域。
2. **本地运营商 DNS 负缓存**：新增/变更解析后，广东移动本地 DNS 可能长时间返回空（缓存了 NXDOMAIN），公共 DNS（223.5.5.5 等）正常，等待过期或换公共 DNS 即可。
3. **pnpm 10 构建脚本审批**：`package.json` 已加 `pnpm.onlyBuiltDependencies`（esbuild、vue-demi），npm/pnpm 均可构建。
4. **Cloudflare 免费版在大陆无节点**：默认 Anycast 路由大陆流量到美西（LAX ~167ms RTT），优选方案的本质是手动选择入口 IP。
